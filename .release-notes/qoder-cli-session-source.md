# Qoder CLI 本地会话

<!-- release-target: both -->

## 新增功能

- 在设置中开启 Qoder 可选来源后，除 Qoder IDE 的会话外，现在也会索引在终端里使用 Qoder CLI 产生的本地会话，可搜索、筛选、按项目查看，并阅读用户与助手消息、工具调用记录以及所在的 Git 分支。
- Qoder CLI 会话优先展示其自动生成的标题，在列表中更容易辨认。
- 仅包含工具执行记录、没有实际对话内容的轨迹不再作为会话出现，避免无标题的空条目干扰搜索结果。
